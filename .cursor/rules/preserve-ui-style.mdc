---
description: 禁止破坏 Fusion Space 既有网页视觉与布局风格
alwaysApply: true
---

# UI / 视觉风格（硬性约束）

对本仓库做任何修改时，**不得破坏**当前产品已确立的前端风格与版式，除非用户在该次对话中**明确**要求改版。

## 必须保持

- **整体气质**：浅色、白底、Notion 式克制排版；以 `globals.css` 与 Tailwind 主题色（`bg`、`ink`、`muted`、`line`、`line-card`、`shell` 等）为准，不擅自改为深色主色、高饱和主题或全新设计体系。
- **首页**：双栏 `line-card` 布局、无整页顶栏（仅右上角轻量 Log in / Sign up 文案链）；**不要**再加回厚重顶栏或大号双按钮登录区。
- **项目内顶栏**：`TopNav` 保持 **Menu + Fusion Space 标题 + Search + Grid + 右侧轻量账户区**；图标**不要**在 `sm` 断点下隐藏（与历史一致）。
- **改动范围**：为实现功能而改 `src/components`、`src/app` 下页面时，**优先**在现有 class 与结构上增量修改；**禁止**为「顺手美化」而批量替换 class、改圆角体系、改字号阶梯或重写整页布局。

## 若功能必须动到 UI

- 新控件须复用现有 `line-card`、`rounded-xl`、`border-line`、`text-muted` 等模式，使新旧区域**视觉同一套语言**。
- 若存在歧义，**默认选择更接近现有页面的实现**，而不是「更炫」或「更不一样」的实现。

## 例外

仅当用户**逐条说明**要改版（例如「把首页改成深色」）时，方可偏离以上约束。
